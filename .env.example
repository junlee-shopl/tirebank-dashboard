# Shopl API authKey (타이어뱅크 워크스페이스 발급분)
# 발급처: 샤플 백오피스
# 보안: 절대 git에 커밋하지 말 것. .env 파일은 .gitignore에 등록됨.
SHOPL_AUTH_KEY=

# 로컬 dev 시 백엔드 포트 (기본 3001). Render 에서는 PORT 환경변수 자동 주입.
PORT=3001

# 가상 데이터 모드 (디자인 시연용 12개 매장/직원). default true.
# 실데이터 (Shopl API 호출) 모드로 전환하려면 false 로 명시.
USE_MOCK_DATA=true
